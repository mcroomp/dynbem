# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.pdb
.Python

# Distribution / packaging
*.egg-info/
*.egg
dist/
build/
wheels/

# Virtual environments
.venv/
venv/
env/

# Testing
.pytest_cache/
.coverage
htmlcov/

# IDE
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db

# Output artifacts
out/
*.npz

# Rust build artifacts (workspace target dir at root; the per-crate
# target/ dirs only appear if someone runs cargo from inside a crate).
target/

# Cargo lockfile: this workspace publishes a library crate (dynbem),
# so we let downstream consumers resolve their own lock per Cargo
# convention.
Cargo.lock

# Editor scratch
Untitled.md

# One-off / scratch scripts (kept on disk, not tracked)
oneoff/


# OCR work-in-progress
Research/ocr/
Research/csv/ocr/
